output_type: generate_until
generation_kwargs:
  until:
    - "<eos>"
    - "&&NOENT&&"
  max_gen_toks: 512

metric_list:
  - metric: f1
    higher_is_better: True
    aggregation: !function metrics._aggreg_ner

metadata:
  version: 1

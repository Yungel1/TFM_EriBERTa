include: _ner_template.yaml

task_alias: 'SympTEMIST: Entity List'
tag: symptemist-ner
task: symptemist-entity_list

dataset_path: json
dataset_name: null
dataset_kwargs:
  data_files:
    test: "datasets/NER/SympTEMIST/test/test.json"

test_split: test

process_docs: !function ner_symptemist_process.process_dataset

doc_to_text: "Extrae todas las entidades relacionadas con síntomas en el siguiente texto. Clasifícalas bajo la categoría única:
SINTOMA: Cualquier manifestación clínica o hallazgo patológico presente en el paciente (ej. 'tumoración en polo superior de teste', 'masa nodular', 'formación nodular').
Devuelve cada entidad en el formato: Entidad$SINTOMA, separando cada par con ','. Si no hay entidades para extraer, responde con '&&NOENT&&'.
Texto: '{{text}}'

Recuerda añadir '$SINTOMA' después de cada entidad extraída y responder exactamente '&&NOENT&&' si no hay entidades."

gen_prefix: "Entidades extraídas: "

doc_to_target: target
process_results: !function ner_symptemist_process.ner_process_results
